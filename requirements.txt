# Reproducible install for the DoubleAdapt TWSE pipeline.
#
#   python -m venv .venv
#   .venv/Scripts/python.exe -m pip install --upgrade pip
#   .venv/Scripts/python.exe -m pip install -r requirements.txt
#
# The --extra-index-url below pulls PyTorch from the NVIDIA CUDA 12.8 wheel
# index (works for Blackwell GPUs like RTX 50-series and any newer CUDA card).
# For CPU-only or older GPUs, change to https://download.pytorch.org/whl/cpu
# (or https://download.pytorch.org/whl/cu124 for older CUDA).
--extra-index-url https://download.pytorch.org/whl/cu128

# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# HTTP for the TWSE API scraper
requests>=2.31

# Deep learning (CUDA 12.8 build via the index URL above)
torch>=2.10

# Progress bars
tqdm>=4.66

# NOTE: the local coursework package `stock_api` is not in this requirements
# file because it lives in a separate repo. After `pip install -r requirements.txt`,
# run the following from the project root:
#   git clone https://ciot.imis.ncku.edu.tw:25388/Amy/stock_project_for_class.git
#   pip install -e ./stock_project_for_class
